# Copyright 2019 The ANGLE Project Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# Override for angle_root
angle_root = "${skia_root_dir}/"

# True if ANGLE can access build/, testing/ and other Chrome folders.
angle_has_build = true

# Overrides for ANGLE's dependencies
angle_abseil_cpp_dir = "${skia_third_party_dir}/abseil-cpp"
angle_glslang_dir = "${skia_third_party_dir}/vulkan-deps/glslang/src"
angle_googletest_dir = "${skia_third_party_dir}/googletest/src"
angle_jsoncpp_dir = "${skia_third_party_dir}/jsoncpp"
angle_libjpeg_turbo_dir = "${skia_third_party_dir}/libjpeg_turbo"
angle_libpng_dir = "${skia_third_party_dir}/libpng"
angle_spirv_cross_dir = "${skia_third_party_dir}/vulkan-deps/spirv-cross/src"
angle_spirv_headers_dir = "${skia_third_party_dir}/vulkan-deps/spirv-headers/src"
angle_spirv_tools_dir = "${skia_third_party_dir}/vulkan-deps/spirv-tools/src"
angle_vulkan_memory_allocator_dir = "${skia_third_party_dir}/vulkan_memory_allocator"
